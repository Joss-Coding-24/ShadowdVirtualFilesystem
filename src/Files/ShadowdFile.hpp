#pragma once
#include <vector>
#include <cstdint>

class ShadowdFile{
  public:
    std::vector<uint8_t> removeAndGetToLast(size_t end){
      std::vector<uint8_t> last;
      return last;
    }
};

using SFile = ShadowdFile;